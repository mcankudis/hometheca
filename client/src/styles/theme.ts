import { createTheme } from '@suid/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#93C144'
        },
        secondary: {
            main: '#EFDEB8'
        },
        info: {
            main: '#494949'
        }
    }
});
